# Evening Call Summary — DA4

The evening discussion centered on whether the explainer was clearly separating the two uncertainty sources in Kidane's result instead of blending them together. Kidane's main feedback was that the piece needed to say more explicitly that a confidence interval only addresses sampling noise, while repeated use of the same held-out split creates a different kind of optimism that the interval does not capture. I revised the explainer so the Wilson interval calculation and the adaptive-reuse argument are treated as two separate layers of the answer, and I added a more explicit reporting recommendation for `MODEL_CARD.md` so the outcome was not only conceptual. By the end of the call, the main point landed cleanly: the 86.0% number is still useful, but it should be treated and reported as a reused validation estimate rather than a final test-set generalization claim.
