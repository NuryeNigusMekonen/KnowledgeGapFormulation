# Final Question

In my Week 11 evaluation artifacts for TheConversionEngine, I have two different statistical stories about improvement, and I cannot defend which one is actually valid. In `ablation_results.json`, `delta_a` reports a 16-point lift for `full_method` over `day1_baseline` with `p_value: 0.018` using a `two_proportion_z_test`. But in `outputs/reports/current_ablation_report.json`, the same kind of comparison is treated as a paired comparison over overlapping tasks, with a `paired_bootstrap_ci + exact_mcnemar`, and on the tiny local trace slice the result shows `paired_task_count: 2` and `p_value: 1.0`. I can see the numbers, but I cannot explain which framing is statistically appropriate or why the choice of test changes the story so dramatically.

My gap is this: when evaluating an agent or judge on overlapping tasks, when should I treat the result as an unpaired proportion comparison and when should I treat it as a paired comparison? More specifically, why are paired-bootstrap confidence intervals and exact McNemar-style tests better suited than a two-proportion z-test when the same tasks are evaluated under two conditions, and what breaks when I ignore that dependence? I also want to understand what these tests are actually estimating, what assumptions they rely on, and what I am allowed to claim when the paired sample is very small.

This matters because I am using these Week 11 evaluation artifacts to justify whether a trained Path B critic really improved the system. If I cannot explain why one statistical test is valid and another is misleading, then I cannot honestly defend the lift claim in `ablation_results.json`, the evaluation write-up in `method.md`, or the decision to treat the critic as production-relevant rather than just promising on a tiny slice.
